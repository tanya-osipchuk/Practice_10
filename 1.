def fib(n):
    if n <= 1:
        return n
    else:
        return fib(n-1) + fib(n-2)
    
a = int(input("Введите номер искомого эллемента: "))    
val = fib(a)
print("Введенный элемент последовательности равен: ", val)
