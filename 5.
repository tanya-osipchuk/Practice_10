def fib(n):
    a = 0
    b = 1

    for i in range(n):
        a, b = b, a + b
    return a

a = int(input("Введите номер искомого эллемента: "))    
value = fib(a)
print("Введенный элемент последовательности равен: ", value)
